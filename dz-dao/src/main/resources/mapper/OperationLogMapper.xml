<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dz.tavern.dao.mapper.OperationLogMapper">
    <insert id="insertOperationLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log(admin_id, module, action, params_digest, ip, cost_ms,
                                  create_time, update_time, deleted)
        VALUES (#{adminId}, #{module}, #{action}, #{paramsDigest}, #{ip}, #{costMs},
                NOW(), NOW(), 0)
    </insert>
    <sql id="conditions">
        deleted = 0
        <if test="adminId != null">AND admin_id = #{adminId}</if>
        <if test="module != null and module != ''">AND module = #{module}</if>
        <if test="startTime != null">AND create_time &gt;= #{startTime}</if>
        <if test="endTime != null">AND create_time &lt; #{endTime}</if>
    </sql>
    <select id="selectPage" resultType="com.dz.tavern.dao.entity.OperationLogEntity">
        SELECT * FROM operation_log WHERE <include refid="conditions"/>
        ORDER BY id DESC LIMIT #{offset}, #{size}
    </select>
    <select id="countPage" resultType="long">
        SELECT COUNT(*) FROM operation_log WHERE <include refid="conditions"/>
    </select>
</mapper>
